[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "uvot_pipeline"
version = "0.1.0"
description = "Swift UVOT transient detection pipeline"
requires-python = ">=3.12"

[tool.setuptools]
packages = ["uvot_pipeline"]